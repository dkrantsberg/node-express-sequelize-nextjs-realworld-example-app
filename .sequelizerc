// Register the tsx loader so sequelize-cli (a plain node process) can read the
// TypeScript config at front/config.ts.
require('tsx/cjs')

const path = require('path')

module.exports = {
  config: path.resolve(path.join('front', 'config.ts')),
}
